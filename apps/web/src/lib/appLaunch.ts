/** Custom URL scheme — registered by the installed Q desktop app (Tauri). */
export const Q_APP_OPEN_URL = "qdj://open";

/** Opens booth app and surfaces the Start gig prompt (session + QR). */
export const Q_APP_START_GIG_URL = "qdj://start-gig";
